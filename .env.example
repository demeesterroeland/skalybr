# Calibre Library Storage Path
CALIBRE_BASE_DIR=/path/to/calibre/libraries
DEFAULT_LIBRARY=Main Library

# Maximum library ZIP upload/download size in MB (default: 1024 = 1GB)
MAX_UPLOAD_SIZE_MB=1024
